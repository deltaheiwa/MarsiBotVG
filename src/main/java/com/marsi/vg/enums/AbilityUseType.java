package com.marsi.vg.enums;

/**
 * Ability can be used in two ways:
 * - PHYSICAL: the ability can be used only when the user is in the same location as the target
 * - REMOTE: the ability can be used from any location
 */
public enum AbilityUseType {
    PHYSICAL,
    REMOTE
}
